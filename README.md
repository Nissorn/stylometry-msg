
---

# 🛡️ Thai-Stylometry V2: Secure Chat with Continuous Identity Verification

โปรเจควิจัยสำหรับรายวิชา **Digital Identification** ของคณะวิศวกรรมศาสตร์คอมพิวเตอร์ มหาวิทยาลัยเกษตรศาสตร์ วิทยาเขตศรีราชา (KU SRC) พัฒนาโดย **นิศรณ์ กรวิโยธิน (CyberGeek Club President)**

แอปพลิเคชันแชทที่ไม่ได้มีแค่ความปลอดภัยในการเข้าสู่ระบบ แต่ยังมีระบบ **Continuous Authentication** ที่ใช้ AI (Attention Char-CNN และ Stacked Ensembles) ตรวจสอบพฤติกรรมการพิมพ์ (Stylometry) ของผู้ใช้ตลอดเวลาเพื่อป้องกันการสวมรอย

## ✨ ฟีเจอร์หลัก (Key Features)

* **Real-time Chat:** รับ-ส่งข้อความทันทีผ่าน WebSocket
* **Identity Security:** ระบบวิเคราะห์สไตล์การพิมพ์ด้วย AI ทุกๆ 5 ข้อความ (Rolling Window)
* **MFA (2FA):** รองรับการล็อกอินสองชั้นผ่านแอป Authenticator (TOTP)
* **Data Encryption:** ข้อความแชทถูกเข้ารหัสด้วย Fernet (AES-128) ก่อนลงฐานข้อมูล SQLite
* **Security Dashboard:** กราฟ Radar Chart แสดงพฤติกรรมการพิมพ์และระดับความน่าเชื่อถือ (Trust Score) แบบเรียลไทม์
* **Audit Logging:** บันทึก IP Address และ User-Agent เพื่อประเมินความเสี่ยงในการเข้าใช้งาน

---

## 🚀 ขั้นตอนการติดตั้งและเริ่มใช้งาน

### 1. เตรียมความพร้อม (Prerequisites)

* **Python:** แนะนำให้ใช้ **Anaconda** หรือ **Miniconda**
* **Node.js:** สำหรับรัน Astro Frontend
* **Git:** สำหรับจัดการ Version Control

### 2. ตั้งค่า Backend (FastAPI)

เปิด Terminal และเข้าไปที่โฟลเดอร์ `backend/`:

```bash
# สร้างและใช้งาน Conda Environment
conda activate did_chat  #

# ติดตั้ง Library ที่จำเป็น
pip install fastapi uvicorn bcrypt cryptography python-multipart pyotp qrcode websockets python-jose[cryptography] #

# เริ่มทำงานเซิร์ฟเวอร์
uvicorn main:app --reload  #

```

*ระบบจะรันอยู่ที่: `http://localhost:8000*`

### 3. ตั้งค่า Frontend (Astro + React)

เปิด Terminal อีกหน้าต่างหนึ่งและเข้าไปที่โฟลเดอร์ `frontend/`:

```bash
# ติดตั้ง dependencies
npm install  #

# เริ่มทำงานโหมด Development
npm run dev  #

```

*แอปพลิเคชันจะพร้อมใช้งานที่: `http://localhost:4321*`

---

## 📂 โครงสร้างโปรเจค (Project Structure)

* **`backend/`**:
* `main.py`: จุดศูนย์กลาง API, WebSocket และ Middleware
* `logic/`: ระบบจัดการฐานข้อมูล (`database.py`) และ AI โมเดล (`mock_ai.py`)
* `models/`: ที่เก็บไฟล์โมเดล Attention Char-CNN (.pt/.h5)


* **`frontend/`**:
* `src/pages/`: ระบบ Routing ของ Astro (Login, Chat)
* `src/components/`: ส่วนประกอบ UI แบบ React (Sidebar, ChatWindow, SecurityPanel)
* `src/store/`: จัดการสถานะ Global ด้วย Zustand



---

## 🛡️ ความปลอดภัยของข้อมูล (Data Policy)

โปรเจคนี้เน้นความปลอดภัยระดับวิศวกรรมคอมพิวเตอร์:

* **Password Hashing:** ใช้ `bcrypt` โดยตรงเพื่อความปลอดภัยสูงสุด
* **Token Storage:** ใช้ **HttpOnly Cookies** ป้องกันการขโมยสิทธิ์ผ่าน XSS
* **CORS Guard:** อนุญาตเฉพาะ Origin ที่กำหนดเพื่อป้องกันการดึงข้อมูลจากเว็บอื่น

---
